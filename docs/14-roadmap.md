# ロードマップ（2026-08-05更新）

外部評価（ArcGIS/CKAN/FME/CARTOとの比較）を反映し、P0→P1→P2の優先度で整理する。狙いは「日本の土木建設公開データを、最短で業務判断材料へ変換する共通基盤」。

## ✅ 実装済み（2026-08-05時点）

| 領域 | 実装 |
| --- | --- |
| データソース台帳 | 56件シード、検索、品質スコア、接続確認、サンプル取得 |
| 後続API | `/api/v1/records/search` `/point` `/layers` `/freshness` |
| 地点横断評価 | `/api/v1/assessments/point`（PostGISカテゴリ別集計・最短距離） |
| データ収集パイプライン | 定期ジョブ・実行履歴・ETag/Last-Modified・リトライ・CSV/GeoJSON/JSON解析・upsert |
| クレンジング | 日付/数値/座標（Web Mercator簡易変換）正規化・dedupe key・欠損/上限チェック |
| データリネージュ | `/api/v1/sources/{id}/lineage`（出典→ジョブ→実行→標準レコード） |
| AIコンシェルジュ | `/api/v1/recommendations`（ルールベース推薦・根拠付き） |
| GISビューア | レイヤー重ね合わせ・凡例・透明度・GeoJSON/CSV出力・距離/面積計測 |

## P0（進行中・次サイクル優先）

1. 実データ収集パイプラインの本格化
   - データ提供元別レート制御、デッドレターキュー、差分取得の実データ適用
   - 対象データセット50〜100種類への展開（台帳500〜1,000件へ拡充）
2. 標準化・クレンジングエンジン強化
   - EPSG判定・座標系変換ライブラリ（proj4）、文字コード自動判定、スキーマドリフト検出、品質前後比較
3. GISビューア本格化
   - 円・矩形・ポリゴン範囲検索、バッファ/交差/包含/最近傍、属性検索、時系列スライダー、GeoPackage出力、地図PDF

## P1（次サイクル以降）

- 地点横断検索の拡張（標高・洪水・土砂・河川・交通・都市計画・避難所・気象警報の統合レスポンス）
- AIコネクタ自動生成（OpenAPI/HTML/PDF仕様→Zod/コネクタ雛形、人間承認フロー）
- AIデータ品質監視（更新停止・件数急変・座標異常・スキーマ/単位変更の検出）
- 通知・ウォッチリスト（メール/Teams/Slack/Webhook、API停止・更新遅延・ライセンス/スキーマ変更）
- RBAC拡張（閲覧者/データスチュワード/コネクタ管理者/品質承認者/監査担当/API利用者）

## P2（基盤拡張）

- CKAN/ArcGIS Hubインポーター、DCAT-AP-JP、OGC API Features、WMS/WMTS/WFS、GeoParquet/STAC、PLATEAU 3D Tiles
- 全文検索（pg_trgm/日本語形態素）、ベクトル検索、ファセット、保存済み検索
- ダッシュボード作成、ノーコード分析テンプレート、データセット比較、品質ルールビルダー
- データ利用申請・承認、APIキー発行・利用量管理、Webhook/SDK生成、PWA現場ビュー

## 完了条件（次のマイルストーン）

- 実データ50種類の定期収集・標準化・地点横断評価がCI検証付きで稼働
- 台帳500件以上、標準レコード10万件以上
- AIコンシェルジュが「地点＋工事シナリオ」から推奨データセットと根拠URLを返し、地図レイヤー追加まで1操作で到達
